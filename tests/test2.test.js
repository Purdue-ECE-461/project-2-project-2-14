const API = require('../APIpackages.js');

describe("array lengths and values", () => {
    // test stuff
    test("it should filter by a search term (link)", () => {
        // actual test
        expect(len(scoreArray)).toBe(7);
      });
  });